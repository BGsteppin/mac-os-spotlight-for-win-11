<Window x:Class="WinSpotlight.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:Controls="clr-namespace:System.Windows.Controls;assembly=PresentationFramework"
        xmlns:search="clr-namespace:WinSpotlight.Search"
        mc:Ignorable="d"
        Title="Spotlight"
        Width="720"
        Height="420"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#CC1F1F1F"
        FontFamily="Segoe UI Variable"
        Loaded="Window_Loaded"
        KeyDown="Window_KeyDown"
        Deactivated="Window_Deactivated">
    <Window.Effect>
        <DropShadowEffect BlurRadius="16" ShadowDepth="0" Opacity="0.35" />
    </Window.Effect>
    <Border CornerRadius="16" Background="#66000000">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Border Height="48" CornerRadius="12" Background="#22000000" Padding="12" BorderBrush="#33FFFFFF" BorderThickness="1">
                <DockPanel>
                    <Viewbox Width="20" Height="20">
                        <Canvas Width="24" Height="24">
                            <Ellipse Width="12" Height="12" Stroke="White" StrokeThickness="1.4" Canvas.Left="3" Canvas.Top="3" />
                            <Line X1="14" Y1="14" X2="20" Y2="20" Stroke="White" StrokeThickness="1.4" StrokeEndLineCap="Round" />
                        </Canvas>
                    </Viewbox>
                    <TextBox x:Name="SearchBox"
                             Margin="8,0,0,0"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="White"
                             FontSize="20"
                             VerticalContentAlignment="Center"
                             TextChanged="SearchBox_TextChanged"
                             KeyDown="SearchBox_KeyDown"
                             SpellCheck.IsEnabled="False"
                             Focusable="True"
                             Controls:TextBoxBase.SelectionBrush="#55FFFFFF">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8" />
                            </Style>
                        </TextBox.Resources>
                    </TextBox>
                </DockPanel>
            </Border>
            <ListView Grid.Row="1" x:Name="ResultsList"
                      Margin="0,12,0,0"
                      Background="Transparent"
                      BorderThickness="0"
                      Foreground="White"
                      SelectionMode="Single"
                      MouseDoubleClick="ResultsList_MouseDoubleClick">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="Padding" Value="10" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListViewItem">
                                    <Border CornerRadius="10" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                        <ContentPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#22FFFFFF" />
                                        </Trigger>
                                        <Trigger Property="IsSelected" Value="True">
                                            <Setter Property="Background" Value="#33FFFFFF" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate DataType="{x:Type search:SearchResult}">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="40" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Image Width="32" Height="32" Source="{Binding Icon}" Stretch="Uniform" />
                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Text="{Binding Title}" FontWeight="SemiBold" FontSize="16" />
                                <TextBlock Text="{Binding Subtitle}" Opacity="0.7" FontSize="12" TextTrimming="CharacterEllipsis" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Border>
</Window>
